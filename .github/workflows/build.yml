name: FarmeronAddress CI

on:
  push:
    branches: master

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Dockerhub login
        env:
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        run: 
          echo "${DOCKER_PASSWORD}" | docker login --username ${DOCKER_USERNAME} --password-stdin
      
      - name: Set up Docker Buildx
        id: buildx
        uses: crazy-max/ghaction-docker-buildx@v1

      - name: Build Manifest
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }} 
          DOCKER_CLI_EXPERIMENTAL: enabled
        run: |
          docker manifest create $(echo "${DOCKER_USERNAME}")/farmerzon-address:latest \
          --amend $(echo "${DOCKER_USERNAME}")/farmerzon-address:latest-amd64 \ 
          --amend $(echo "${DOCKER_USERNAME}")/farmerzon-address:latest-arm64v8
          docker manifest push $(echo "${DOCKER_USERNAME}")/farmerzon-address:latest"